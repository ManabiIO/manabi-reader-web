name: Ttu migration qualification
on:
  pull_request:
    paths:
      - 'apps/web/**'
      - 'tests/**'
      - '.github/workflows/ttu-migration.yml'
permissions:
  contents: read
concurrency:
  group: ttu-migration-${{ github.ref }}
  cancel-in-progress: true
jobs:
  migration:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@v6.1.0
        with:
          version: '12.3.4'
      - uses: actions/setup-node@v4
        with:
          node-version: '24.21.0'
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --dir apps/web exec svelte-kit sync
      - run: pnpm --dir apps/web check
      - name: Preserve exact public review inputs
        run: |
          tar -czf "$RUNNER_TEMP/migration-workspace.tar.gz" --exclude=.git --exclude=.svelte-kit --exclude=build --exclude=assets/readme .
      - uses: actions/upload-artifact@v4
        with:
          name: migration-workspace
          path: ${{ runner.temp }}/migration-workspace.tar.gz
          retention-days: 1
