name: Foliate layered slide
on:
  pull_request:
    paths:
      - 'apps/web/**'
      - 'tests/browser/test_foliate_slide.py'
      - 'tests/browser/test_static_reader.py'
      - 'tests/browser/test_epub_publication.py'
      - 'tests/unit/foliate-*.test.mjs'
      - '.github/workflows/foliate-slide.yml'
  push:
    branches: [main]
    paths:
      - 'apps/web/**'
      - 'tests/browser/test_foliate_slide.py'
      - 'tests/browser/test_static_reader.py'
      - 'tests/browser/test_epub_publication.py'
      - 'tests/unit/foliate-*.test.mjs'
      - '.github/workflows/foliate-slide.yml'
permissions:
  contents: read
concurrency:
  group: foliate-slide-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true
jobs:
  slide:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, webkit]
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@v6.1.0
        with:
          version: '12.3.4'
      - uses: actions/setup-node@v4
        with:
          node-version: '24.21.0'
          cache: pnpm
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: pnpm install --frozen-lockfile
      - run: pnpm --dir apps/web exec svelte-kit sync
      - run: pnpm --dir apps/web check
      - run: node --test tests/unit/foliate-*.test.mjs
      - name: Build the real reader
        env:
          BASE_PATH: /reader-web
        run: pnpm build
      - name: Exercise gestures and capture keyframes
        env:
          SLIDE_BROWSER: ${{ matrix.browser }}
        run: |
          python -m pip install 'playwright==1.55.0'
          python -m playwright install --with-deps ${{ matrix.browser }}
          set -o pipefail
          python tests/browser/test_foliate_slide.py 2>&1 | tee foliate-slide.log
      - name: Qualify resource imports, continuous reading and legacy records
        env:
          SLIDE_BROWSER: ${{ matrix.browser }}
        run: |
          set -o pipefail
          python tests/browser/test_epub_publication.py 2>&1 | tee epub-publication.log
      - name: Preserve image, Japanese-content and chapter-link coverage
        if: matrix.browser == 'chromium'
        run: |
          set -o pipefail
          python tests/browser/test_static_reader.py \
            ReaderBrowser.test_foliate_paginated_epub_preserves_reader_security_and_japanese_content \
            ReaderBrowser.test_foliate_cross_resource_links_keep_duplicate_fragment_identity \
            2>&1 | tee foliate-regressions.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: foliate-slide-${{ matrix.browser }}
          path: |
            foliate-slide.log
            foliate-regressions.log
            epub-publication.log
            test-results/
          retention-days: 7
