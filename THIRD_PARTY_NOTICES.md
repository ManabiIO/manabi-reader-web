# Manabi Reader for Web — distribution notices

This application includes separately copyrighted components. The original Ttsu/ッツ Reader source retains the BSD-3-Clause license and notices in `LICENSE` and its original files. Newly added ManabiTan integration code is offered under GPL-3.0-or-later; the combined distribution is not represented as BSD-only. ManabiTan's GPL text is retained in `LICENSE-ManabiTan-GPL-3.0.txt` and in the separately versioned runtime artifact.

The exact ManabiTan source revision is recorded in `apps/web/src/lib/dictionary/runtime-lock.json`. The static build fetches/builds that source without requiring a runtime backend. Distributors must provide corresponding source, including their modifications and build instructions, for the exact shipped application and runtime. Retain all dependency licenses; this notice is not a blanket conclusion that every dependency, host or preset dictionary is legally compatible in every distribution.

Jitendex 2026.08.11.0 is separately copyrighted by Stephen Kraus and licensed CC-BY-SA-4.0. It incorporates JMdict (EDRDG), Tatoeba and JmdictFurigana data with their own attribution requirements. Its authoritative notices are at https://jitendex.org/pages/legal.html and are linked in dictionary settings. The static archive is release-pinned and checksum-verified; it is not automatically downloaded by a browser or bundled into shell precaching. Other presets are linked from the shared ManabiTan catalog and require individual license review before redistribution. Custom dictionaries remain the user's locally imported data.

System YuKyokasho/YuKyokasho Yoko fonts are requested only by CSS family name. No Apple font files are distributed. The existing self-hosted Klee One fallback retains its OFL notice at `apps/web/static/legal/Klee-One-OFL.txt`. Dictionary-media conversion fonts belong to the independently versioned ManabiTan runtime and retain that project's notices. Never substitute proprietary system fonts into the hosted asset directory.

UAParser.js v2 and its type package were removed from Reader's declared dependencies; the only previous use was an archive-worker compatibility heuristic. A small feature check plus the existing conservative Android Chromium workaround replaces that use. Unreferenced historical lockfile entries are not application imports; dependency-lock pruning and a complete release-license inventory remain part of release verification.
